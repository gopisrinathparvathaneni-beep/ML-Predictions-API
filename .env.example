# Environment variables for the ML API application

# Path to the Keras model file (relative to the project root)
MODEL_PATH=models/my_classifier_model.h5

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO
 
# Port for the API server
PORT=8000
